// 27.	Use logical OR (||) between two conditions (like marks > 50 OR attendance > 75). Print the result.

let marks = 49;
let attendance = 76;
if(marks > 50 || attendance > 75) {
    console.log("Promoted")
}else {
    console.log("Not Promoted")
}