// 10.	Create a boolean variable (true/false). Use the NOT (!) operator on it and print the output.

let isLoggedIn = true;

console.log("Teams Login", isLoggedIn);
console.log("Teams Logout", !isLoggedIn);