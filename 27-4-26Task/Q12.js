// 12.	Write a program that checks whether a number is even or odd using the modulus (%) operator and prints the result.

for(let i = 1; i <= 20; i++) {
    if (i % 2 === 0) {
        console.log(i + " Even");
    }else{
        console.log(i + " Odd")
    }
}